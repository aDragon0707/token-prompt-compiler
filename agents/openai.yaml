interface:
  display_name: "Token Prompt Compiler"
  short_description: "Compile messy intent into Prompt IR, model adapters, and validator-ready packets."
  default_prompt: "Compile my messy request into Prompt IR first, then produce the smallest useful GPT/OpenAI or Claude-ready prompt packet with scope, boundaries, output contract, validator, and stop rule. Use a Tiny Packet or skip the packet if the task is already clear."
